
/*
Public class Rectangles extends Shapes, GetPoints{
  public void getrectArea(){
    GetPoints myPoints = new getPoint()
    this.gx1 

    double rx2 = 
    double ry2 = 
    double ry2 = 
    double glength = (gx2 - gx1);
    double gwidth = (gy2 - gy1);
    double rectArea = glength*gwidth;
    System.out.println("The area of the rectangle is: " + rectArea);
  }
}


public class Rect extends Shapes {

  private final double width, length; //sides

  

  public Rect() {
      this(1,1);
  }
  public Rect(double width, double length) {
      this.width = width;
      this.length = length;
  }

  @Override
  public double area() {
      // A = w * l
      return width * length;
  }

  @Override
  public double circumference() {
      // P = 2(w + l)
      return 2 * (width + length);
  }





*/