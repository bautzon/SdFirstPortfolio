/*public class Rectangles {
    GetPoints myPoints = new GetPoints();
    double rx1 = myPoints.GetPoints(gx1);
    double rx2 = myPoints.GetPoints(gx2);
    double ry2 = myPoints.GetPoints(gy2);
    double ry1 = myPoints.GetPoints(gy1);

  public void rectangles(){
    double glength = (rx2 - rx1);
    double gwidth = (ry2 - ry1);
    double rectArea = glength*gwidth;
    System.out.println("The area of the rectangle is: " + rectArea);
    return;
  }
}


/*
main class 
        Vars varsObject = new Vars();
        int x = varsObject.getNum();
        System.out.println(x);
    }
}
Variable Class:

package getVarTest;

    public class Vars {
        private int num;
            public void setNum(int x){
                this.num = 5;
            }
            public int getNum(){
                return num;
            }
    }

/*
public class Rect extends Shapes {

  private final double width, length; //sides

  

  public Rect() {
      this(1,1);
  }
  public Rect(double width, double length) {
      this.width = width;
      this.length = length;
  }

  @Override
  public double area() {
      // A = w * l
      return width * length;
  }

  @Override
  public double circumference() {
      // P = 2(w + l)
      return 2 * (width + length);
  }





*/